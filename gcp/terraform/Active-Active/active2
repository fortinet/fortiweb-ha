config system ha
    set mode active-active-high-volume
    set group-name group1
    set priority 8
    set tunnel-local ${active2_port3_ip}
    set tunnel-peer ${active1_hb_ip}
    set monitor port3 port1
    set override enable
end